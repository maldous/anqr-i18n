import type { PageDefinition } from '../types'
import { LAST_UPDATED } from '../types'

export const docs: PageDefinition = {
  title: 'ANQR பயனர் வழிகாட்டி',
  description: 'QR கோடுகளை உருவாக்க ANQR பயன்படுத்துவதற்கான முழுமையான வழிகாட்டி.',
  lastUpdated: LAST_UPDATED,
  sections: [
    // Full translation for all sections...
  ],
}